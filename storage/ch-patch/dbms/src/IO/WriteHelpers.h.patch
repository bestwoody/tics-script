diff --git a/dbms/src/IO/WriteHelpers.h b/dbms/src/IO/WriteHelpers.h
index e082cc92dea3..b4efd8506b11 100644
--- a/dbms/src/IO/WriteHelpers.h
+++ b/dbms/src/IO/WriteHelpers.h
@@ -5,6 +5,7 @@
 #include <limits>
 #include <algorithm>
 #include <iterator>
+#include <iomanip>
 
 #include <common/DateLUT.h>
 #include <common/LocalDate.h>
@@ -16,6 +17,7 @@
 #include <Common/Exception.h>
 #include <Common/StringUtils/StringUtils.h>
 #include <Common/UInt128.h>
+#include <Common/Decimal.h>
 #include <common/StringRef.h>
 
 #include <IO/WriteBuffer.h>
@@ -644,6 +646,7 @@ inline void writeBinary(const UInt128 & x, WriteBuffer & buf) { writePODBinary(x
 inline void writeBinary(const UInt256 & x, WriteBuffer & buf) { writePODBinary(x, buf); }
 inline void writeBinary(const LocalDate & x, WriteBuffer & buf) { writePODBinary(x, buf); }
 inline void writeBinary(const LocalDateTime & x, WriteBuffer & buf) { writePODBinary(x, buf); }
+inline void writeBinary(const DecimalValue & x, WriteBuffer & buf) { writePODBinary(x, buf); }
 
 
 /// Methods for outputting the value in text form for a tab-separated format.
@@ -800,4 +803,13 @@ inline String toString(const T & x)
     return buf.str();
 }
 
+template <typename T>
+inline std::enable_if_t<std::is_floating_point_v<T>, String>
+toString(const T & x, int precision)
+{
+    std::stringstream ss;
+    ss << std::fixed << std::setprecision(precision) << x;
+    return ss.str();
+}
+
 }
