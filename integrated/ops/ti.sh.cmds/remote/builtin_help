run (alais: up)
    run defined and selected modules
    usage: <cmd> (no args)
stop (alais: down)
    stop processes of defined and selected modules using 'kill'
    usage: <cmd> (no args)
fstop
    same as 'stop' but using 'kill -9'
    usage: <cmd> (no args)
status (alais: run this cmd when no cmd provided)
    show status of modules
    usage: <cmd> (no args)
